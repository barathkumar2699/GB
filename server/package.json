{
  "name": "gb",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server db.json --watch --port 5000",
    "server": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "ba",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
